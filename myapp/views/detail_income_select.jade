extends layout

block content
  p #{name} welcome || 
    a(href="/logout") logout
  h1=title
  form(action="/account/detail/income/list" method="post")
    select(name="title")
        !{options}
    p
        input(type="submit" value="조회")
